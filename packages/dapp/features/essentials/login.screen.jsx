import { Text, Box } from '@clixpesa/native-base';

const LoginScreen = () => (
  <Box flex={1} bg="primary.100" alignItems="center" justifyContent="center">
    <Text fontSize="xl">Log In Screen!</Text>
  </Box>
);

export default LoginScreen;
